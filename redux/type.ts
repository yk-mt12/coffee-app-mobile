export type CoffeeType = {
  amount: number;
  water: number;
  ratio: number;
  iceAmount: number;
  iceWater: number;
  iceRatio: number;
  isWater: boolean;
};

export type screenType = {
  nowScreen: string;
}