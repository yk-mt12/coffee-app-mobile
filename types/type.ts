export type AmountButtonType = {
  title: string;
  amount: number;
  onPress: () => void;
};

export type AmountInputType = {
  title: string;
};

export type RatioInputType = {
  title: string;
};

export type WaterInputType = {
  title: string;
}

export type WaterViewType = {
  title: string;
};

export type SwitchButtonType = {
  title: string;
}

export type AmountViewType = {
  amount: number;
}